<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Accueil</title>
	</head>
	<body>
		<h1>Bienvenue sur la page d'accueil</h1>
		<p>Ceci est la page d'accueil de votre site Symfony.</p>

		<p>Le chiffre du jour :
			{{ chiffre_du_jour }}</p>

		{% if is_granted('ROLE_ADMIN') %}
			{# Si l'utilisateur possède le rôle 'ROLE_ADMIN' #}
			<p>Bonjour, ô maître !</p>
		{% endif %}

	</body>
</html>
